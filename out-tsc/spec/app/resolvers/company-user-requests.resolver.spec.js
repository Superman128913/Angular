import { TestBed } from '@angular/core/testing';
import { CompanyUserRequestsResolver } from './company-user-requests.resolver';
describe('CompanyUserRequestsResolver', () => {
    let resolver;
    beforeEach(() => {
        TestBed.configureTestingModule({});
        resolver = TestBed.inject(CompanyUserRequestsResolver);
    });
    it('should be created', () => {
        expect(resolver).toBeTruthy();
    });
});
//# sourceMappingURL=company-user-requests.resolver.spec.js.map