// Angular
import {Routes} from '@angular/router';
// Application
import {PressComponent} from './press.component';

export const PRESS_CENTER_ROUTES: Routes = [
    {
        path: '',
        component: PressComponent
    }
];
